package commands

import (
	"context"
	"fmt"
)

// Serve starts the development server with live reload
func Serve(ctx context.Context, args []string) error {
	// TODO: Implement in Stage 8
	fmt.Println("serve command - not yet implemented")
	fmt.Println("Usage: templsite serve [--port <port>] [--addr <address>] [--watch]")
	return nil
}
