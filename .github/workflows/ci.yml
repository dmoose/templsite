name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['1.26']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      # The generator must match the templ runtime pinned in go.mod: a newer
      # CLI emits calls against APIs the pinned library does not have.
      - name: Install templ
        run: |
          version=$(go list -m -f '{{.Version}}' github.com/a-h/templ)
          echo "installing templ $version (pinned by go.mod)"
          go install "github.com/a-h/templ/cmd/templ@$version"

      - name: Generate templ components
        run: templ generate -path ./components

      - name: Build
        run: go build ./...

      - name: Test
        run: go test -race ./...

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      # The generator must match the templ runtime pinned in go.mod: a newer
      # CLI emits calls against APIs the pinned library does not have.
      - name: Install templ
        run: |
          version=$(go list -m -f '{{.Version}}' github.com/a-h/templ)
          echo "installing templ $version (pinned by go.mod)"
          go install "github.com/a-h/templ/cmd/templ@$version"

      - name: Generate templ components
        run: templ generate -path ./components

      - name: Lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: latest

      - name: Check go fix
        run: |
          go fix ./...
          if [ -n "$(git diff)" ]; then
            echo "go fix produced changes -- run 'go fix ./...' locally"
            git diff
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install REUSE tool
        run: pip install reuse

      - name: Check REUSE compliance
        run: reuse lint
